express:
1.主体
２.cookie:
浏览器保存cookie:res.secret=str;res.cookis('name','value',{signed:true});
服务器读取cookies:引入cookie-parser,use(cookieparser());req.cookies/req.signedcookies;
最大4k,不安全（签名）；
保存在浏览器的数据
session:
保存在服务器的数据，依靠cookie存在
3.数据交互post/get
4.模板引擎jade/ejs